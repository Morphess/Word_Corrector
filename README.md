# Word-Corrector

Java console application for correcting words in a given dataset.

## Description
The application:
- receives an array of words
- replaces incorrect characters where necessary
- outputs corrected words in numbered format